# Python dependencies with no apt package for rosdep to resolve.
# Only the nodes that import them need them installed.
#
#   pip install -r requirements.txt

# gps_node -- GNSS reader and NTRIP client
pygnssutils

# segment_model_node -- steering PID
simple-pid

# ekf_localizer_node -- extended Kalman filter
filterpy

# onnx_node, segment_model_node -- model inference
onnxruntime-gpu

# zed_frame_publisher_node -- installed with the ZED SDK, not from pip:
#   https://www.stereolabs.com/docs/app-development/python/install
# pyzed

# gstreamer_video_sync_node -- GStreamer bindings, install from apt:
#   sudo apt install python3-gi gstreamer1.0-plugins-{good,bad,ugly} gstreamer1.0-libav
